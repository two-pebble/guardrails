import { describe, expect, it } from "vitest";

describe("my feature", () => {
  it("should work", () => {
    expect(true).toBe(true);
  });

  it("should also work", () => {
    expect(1 + 1).toBe(2);
  });
});
