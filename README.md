# Guardrails

`guardrails` is a `pnpm` monorepo scaffold with two workspace packages:

- `packages/guardrails`: publishable TypeScript package for GitHub Packages
- `packages/docs`: GitHub Pages site for documentation

This repo currently contains plumbing only so package implementation can be added in follow-up work.

